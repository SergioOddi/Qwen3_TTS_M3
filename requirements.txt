# Core TTS dependencies
qwen-tts>=0.0.5

# Audio processing
soundfile>=0.12.1
pydub>=0.25.1
librosa>=0.10.0
numpy>=1.24.0

# Model downloading (optional)
modelscope>=1.11.0

# Flash Attention 2 (optional - install separately with MAX_JOBS=4)
# flash-attn>=2.0.0

# Utilities
torch>=2.0.0
tqdm>=4.65.0

# App standalone (web UI locale)
fastapi>=0.110.0
uvicorn>=0.30.0
python-multipart>=0.0.9
pywebview>=5.0          # app desktop (finestra nativa, doppio-click)

# Test
pytest>=8.0.0
